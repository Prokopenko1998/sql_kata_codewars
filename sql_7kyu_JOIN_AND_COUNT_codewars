--Для цього завдання вам потрібно створити простий оператор SELECT, який повертатиме всі стовпці з таблиці людей, і приєднатися до таблиці іграшок, щоб ви могли повернути КІЛЬКІСТЬ іграшок

--схема таблиці людей
--id
--назва
--схема столу іграшок
--id
--назва
--ідентифікатор_людей
--Ви повинні повернути всі поля людей, а також кількість іграшок як "toy_count".

--ПРИМІТКА. Ваше рішення має використовувати чистий SQL. Ruby використовується в тестових випадках для фактичного тестування.

--ОПИС:
-- Для цього завдання вам потрібно створити простий оператор SELECT, який повертатиме всі стовпці з таблиці людей,
-- і приєднайтеся до таблиці іграшок, щоб ви могли повернути КІЛЬКІСТЬ іграшок.
-- Ви повинні повернути всі поля людей, а також кількість іграшок як "toy_count".

-- схема таблиці людей
-- ідентифікатор
-- ім'я

-- схема іграшкового столу
-- ідентифікатор
-- ім'я
-- ідентифікатор_людей

SELECT p.id, p.name, COUNT(*) as toy_count FROM people p
JOIN toys t ON p.id = t.people_id
GROUP BY p.id;
